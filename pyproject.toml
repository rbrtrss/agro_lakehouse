[project]
name = "agro-lakehouse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.34",
    "httpx>=0.28",
    "pandas>=2.2",
    "rich>=13.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]
